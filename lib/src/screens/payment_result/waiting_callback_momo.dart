import 'package:flutter/material.dart';

class WaittingCallbackMomo extends StatelessWidget {
  const WaittingCallbackMomo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
